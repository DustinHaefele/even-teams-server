DROP TABLE IF EXISTS even_teams_groups;

CREATE TABLE even_teams_groups (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  group_name TEXT NOT NULL,
  user_id INTEGER
    REFERENCES even_teams_users(id) ON DELETE CASCADE NOT NULL 
);